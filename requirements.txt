# Core engine (required)
pyyaml
jsonschema

# Tests / CI
pytest

# Optional: local model inference (only needed for `python -m compiler.run --local`)
# Works with any OpenAI-compatible server: llama.cpp, Ollama, vLLM, LM Studio.
openai
