# Outputs — Semantic Clustering

Produces: `semantic-ir (clusters)`.

Write `build/<artifact-type>/artifact.json` validated against `artifact-schema.json`, plus `metadata.json` and `diagnostics.json`.
