# Outputs — Taxonomy Construction

Produces: `ontology-ir (hierarchies)`.

Write `build/<artifact-type>/artifact.json` validated against `artifact-schema.json`, plus `metadata.json` and `diagnostics.json`. Deterministic skills also write `evaluation.json`.
